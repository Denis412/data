import { TypesConfig } from '../types';

const config: TypesConfig = {
  order: {
    label: 'order',
    properties: [
      {
        name: 'name',
        data_type: 'text',
      },
    ],
  },
  deal: {
    label: 'deal',
    properties: [
      {
        name: 'name',
        data_type: 'text',
      },
    ],
  },
};

export default config;
